#include "shell.h"

/**
 * calcsum - calulate the sum of two numbers
 * @num1: number received
 * @num2: number received
 *
 * Description -
 * Return: 0
 */
int calcsum(int num1, int num2)
{
int sum = 0;
sum = num1 + num2;
printf("the sum of two numbers is %d:", sum);
return (0);
}
